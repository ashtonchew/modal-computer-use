# Benchmark Results

This directory contains local benchmark evidence that must not be committed, including raw provider output, preregistrations, candidates, rejected runs, and replay inputs.

Only this README is tracked. Promote an accepted result by generating a sanitized, normalized artifact in `benchmark-data/`; never move raw output there or write benchmark output to the repository root.
